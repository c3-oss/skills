[project]
name = "skills"
version = "0.1.0"
description = "agent skills for claude code"
requires-python = ">=3.11"

[tool.uv]
package = false

[dependency-groups]
dev = [
  "gitlint>=0.19.1",
  "pre-commit>=4.6.0",
]
